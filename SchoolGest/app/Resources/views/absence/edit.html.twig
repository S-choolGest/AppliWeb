{% extends 'base.html.twig' %}

{% block container %}
    <center>
        <div class="container" style="width: 300px";>
    <h1>Absence edit</h1>
<br>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class= "btn btn-round btn-warning" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <br>
            {{ form_start(delete_form) }}
            <input class="btn btn-round btn-danger" type="submit" value="Delete">
            {{ form_end(delete_form) }}
       <br>
            <br>
            <a class="btn btn-round btn-info" href="{{ path('absence_index') }}"><i class="fa fa-chevron-circle-left"></i> Back to the list</a>

        </div>
    </center>

{% endblock %}
